name: Project Deployment

on:
  push:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Download project from git
        uses: actions/checkout@v2

      - name: Build project
        run: |
          npm install
          npm run build
